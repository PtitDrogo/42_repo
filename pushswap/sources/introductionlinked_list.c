#include <stdio.h>

typedef struct Node 
{
    int x;
    struct Node   *next;
} Node;

int main(int argc, char *argv[])
{
    Node root, elem2;
    root.x = 15;
    root.next = &elem2;
    elem.x = -2; 
    return (0);
}